"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#092327",
    },
    secondary: {
      main: "#0b5351",
    },
  },
});

export default theme;
